
server:
  port: 8888
spring:
  application:
    name: bcp-bootcamp-configserver

  cloud:
    config:
      server:
        git:
          uri: https://github.com/soabel/bcp-bootcamp-repoconfig.git
          default-label: main
          timeout: 10
